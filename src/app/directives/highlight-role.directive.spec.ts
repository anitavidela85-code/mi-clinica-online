

import { TestBed, async } from '@angular/core/testing';
import { HighlightRoleDirective } from './highlight-role.directive';

describe('Directive: HighlightRole', () => {
  it('should create an instance', () => {
    const directive = new HighlightRoleDirective();
    expect(directive).toBeTruthy();
  });
});
